# Environment variable examples for local development

# Database configuration for the Postgres container
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=uranai_app

# Supabase credentials (if using Supabase instead of local Postgres)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# OpenAI API key for generating horoscope and tarot readings
OPENAI_API_KEY=

# The port your Next.js app runs on (optional override)
PORT=3000

# Connection string used by docker compose (built from the POSTGRES_* values above)
DATABASE_URL=postgresql://postgres:postgres@db:5432/uranai_app
